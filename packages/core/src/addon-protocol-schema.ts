// Compatibility shim: the addon protocol JSON-Schema builder moved to @nicotind/addon-sdk (phase 4 repo split).
// Re-exported so in-monorepo @nicotind/core consumers keep resolving unchanged.
export * from '@nicotind/addon-sdk/addon-protocol-schema';

// Compatibility shim: title matching moved to @nicotind/addon-sdk (phase 4 repo split).
// Re-exported so in-monorepo @nicotind/core consumers keep resolving unchanged.
export * from '@nicotind/addon-sdk/title-match';

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- LEANBACK_LAUNCHER puts the app on the Android/Google TV home
                     row — required even for a sideloaded APK (issue #388). Extra
                     filter categories never un-match phone launchers, so one
                     activity serves both form factors. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Android TV / TV boxes have no touchscreen; without this the OS defaults
         android.hardware.touchscreen to required=true, which blocks/degrades
         install on some TV launchers even for a sideloaded APK. leanback is
         declared optional (required="false") so the SAME APK stays installable
         on phones while the LEANBACK_LAUNCHER category + banner above list it
         on the TV home row (issue #388) — see docs/mobile-app.md. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Background music playback + system controls: @jofr/capacitor-media-session
         runs a media-playback foreground service so audio keeps playing when
         backgrounded and the media notification / lock-screen controls show.
         POST_NOTIFICATIONS surfaces the notification; WAKE_LOCK keeps the CPU awake
         while streaming. See docs/mobile-app.md "Background audio". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- QR device pairing: @capacitor/barcode-scanner opens the camera to scan
         the desktop's Link-a-device QR. See docs/device-pairing.md. -->
    <uses-permission android:name="android.permission.CAMERA" />
</manifest>
